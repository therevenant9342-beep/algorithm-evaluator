# ==========================================
# Algorithm Performance Evaluator Dependencies
# Run: pip install -r requirements.txt
# ==========================================

# 1. CustomTkinter: Required to build the modern, dark-themed GUI 
# that matches the project mockup requirements.
customtkinter>=5.2.0

# 2. Matplotlib: Required to generate the visual chart plotting 
# execution time vs. input size in the Results Panel.
matplotlib>=3.8.0

# 3. NumPy: Required for the math behind the Complexity Estimation Engine 
# (e.g., curve fitting to detect O(n), O(n^2), etc.).
numpy>=1.26.0